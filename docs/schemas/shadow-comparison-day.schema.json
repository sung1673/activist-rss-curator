{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://news.bside.ai/schemas/shadow-comparison-day.schema.json",
  "title": "BSIDE shadow comparison day evidence",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "date",
    "environment",
    "evidence_source",
    "is_synthetic",
    "collected_at",
    "code_revision",
    "legacy_run",
    "candidate_run",
    "legacy_crosswalk",
    "discrepancies_reviewed"
  ],
  "properties": {
    "schema_version": {"const": 1},
    "date": {"type": "string", "format": "date"},
    "environment": {"const": "production"},
    "evidence_source": {"type": "string", "minLength": 1},
    "is_synthetic": {"const": false},
    "collected_at": {"type": "string", "format": "date-time"},
    "code_revision": {"type": "string", "pattern": "^[0-9a-fA-F]{7,64}$"},
    "legacy_run": {"$ref": "#/$defs/engineRun"},
    "candidate_run": {"$ref": "#/$defs/engineRun"},
    "legacy_crosswalk": {"$ref": "#/$defs/legacyCrosswalk"},
    "discrepancies_reviewed": {"type": "boolean"}
  },
  "$defs": {
    "engineRun": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "events"],
      "properties": {
        "status": {"type": "string", "enum": ["succeeded", "failed"]},
        "events": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["comparison_key"],
            "properties": {
              "comparison_key": {"type": "string", "minLength": 1}
            },
            "additionalProperties": true
          }
        }
      }
    },
    "legacyCrosswalk": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schema_version",
        "eligible_legacy_record_count",
        "crosswalked_legacy_record_count",
        "unmatched_legacy_record_count",
        "ambiguous_legacy_record_count",
        "coverage_rate",
        "crosswalk_sha256"
      ],
      "properties": {
        "schema_version": {"const": 1},
        "eligible_legacy_record_count": {"type": "integer", "minimum": 1},
        "crosswalked_legacy_record_count": {"type": "integer", "minimum": 1},
        "unmatched_legacy_record_count": {"const": 0},
        "ambiguous_legacy_record_count": {"const": 0},
        "coverage_rate": {"const": 1.0},
        "crosswalk_sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"}
      }
    }
  }
}
