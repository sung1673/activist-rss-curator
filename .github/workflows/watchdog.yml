name: Operations watchdog

on:
  schedule:
    # A five-minute probe records real Pages/API availability and keeps
    # web-distribution failure detection within ten minutes.
    - cron: "1,6,11,16,21,26,31,36,41,46,51,56 * * * *"
  workflow_dispatch:

permissions:
  contents: read
  issues: write

concurrency:
  group: operations-watchdog
  cancel-in-progress: true

jobs:
  health:
    if: >-
      github.ref_name == github.event.repository.default_branch &&
      (
        github.event_name == 'workflow_dispatch' ||
        vars.GOVERNANCE_PIPELINE_MODE == 'dart_canary' ||
        vars.GOVERNANCE_PIPELINE_MODE == 'shadow' ||
        vars.GOVERNANCE_PIPELINE_MODE == 'live' ||
        (vars.GOVERNANCE_PIPELINE_MODE == '' && vars.ENABLE_GOVERNANCE_SHADOW == 'true')
      )
    runs-on: ubuntu-latest
    timeout-minutes: 5
    environment:
      name: governance-runtime
    steps:
      - name: Initialize generic incident evidence
        id: incident_evidence
        shell: bash
        run: |
          set -euo pipefail
          report="$RUNNER_TEMP/watchdog-generic-incident-report.md"
          echo "report_path=$report" >> "$GITHUB_OUTPUT"
          cat > "$report" <<'EOF'
          # Governance pipeline watchdog

          **INCIDENT**

          The watchdog could not complete one or more setup, routing, or health checks.
          Inspect this workflow run; no configured URL or credential was written to this report.
          EOF

      - name: Checkout
        id: checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.sha }}

      - name: Verify immutable workflow revision
        id: revision_guard
        shell: bash
        run: |
          set -euo pipefail
          actual="$(git rev-parse HEAD)"
          [[ "$actual" == "$GITHUB_SHA" ]] || {
            echo "::error::Checked-out revision $actual does not match workflow revision $GITHUB_SHA."
            exit 1
          }

      - name: Resolve fail-closed rollout mode
        id: rollout
        env:
          PAGES_OWNER: ${{ vars.PAGES_OWNER }}
          GOVERNANCE_PIPELINE_MODE: ${{ vars.GOVERNANCE_PIPELINE_MODE }}
          ENABLE_PAGES: ${{ vars.ENABLE_PAGES }}
          ENABLE_GOVERNANCE_PAGES: ${{ vars.ENABLE_GOVERNANCE_PAGES }}
          ENABLE_GOVERNANCE_SHADOW: ${{ vars.ENABLE_GOVERNANCE_SHADOW }}
          ENABLE_TELEGRAM_DELIVERY: ${{ vars.ENABLE_TELEGRAM_DELIVERY }}
          ENABLE_GOVERNANCE_DELIVERY: ${{ vars.ENABLE_GOVERNANCE_DELIVERY }}
        run: python -m curator.operation_mode --github-output "$GITHUB_OUTPUT"

      - name: Validate operational API routing
        id: api_routing
        env:
          BSIDE_API_BASE_URL: ${{ secrets.BSIDE_API_BASE_URL || vars.GOVERNANCE_API_BASE_URL }}
          GOVERNANCE_API_BASE_URL: ${{ vars.GOVERNANCE_API_BASE_URL }}
        shell: bash
        run: |
          set -euo pipefail
          report="$RUNNER_TEMP/watchdog-routing-report.md"
          echo "report_path=$report" >> "$GITHUB_OUTPUT"
          cat > "$report" <<'EOF'
          # Governance pipeline watchdog

          **INCIDENT**

          The watchdog could not complete its operational API routing or execution checks.
          Inspect this workflow run; no configured URL or credential was written to this report.
          EOF
          python .github/scripts/validate-api-base-urls.py --github-env "$GITHUB_ENV"

      - name: Inspect ingest freshness and web availability
        id: health
        env:
          BSIDE_PUBLIC_WEB_URL: ${{ vars.BSIDE_PUBLIC_WEB_URL || 'https://news.bside.ai' }}
          BSIDE_OPS_TOKEN: ${{ secrets.BSIDE_OPS_TOKEN }}
          WATCHDOG_MAX_INGEST_AGE_MINUTES: "90"
          WATCHDOG_GOVERNANCE_PAGES: ${{ steps.rollout.outputs.scheduled_governance_enabled }}
          WATCHDOG_EXPECTED_OFFICIAL_SOURCES: ${{ steps.rollout.outputs.governance_pipeline_mode == 'dart_canary' && 'dart' || 'dart,kind' }}
        run: python .github/scripts/watchdog.py

      - name: Create, update, or resolve the incident issue
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          INCIDENT: ${{ (steps.checkout.outcome == 'failure' || steps.revision_guard.outcome == 'failure' || steps.rollout.outcome == 'failure' || steps.api_routing.outcome == 'failure' || steps.health.outcome == 'failure' || steps.health.outputs.incident == 'true') && 'true' || 'false' }}
          REPORT_PATH: ${{ steps.health.outputs.report_path || steps.api_routing.outputs.report_path || steps.incident_evidence.outputs.report_path }}
        shell: bash
        run: |
          set -euo pipefail
          gh label create "ops/incident" \
            --repo "$GH_REPO" \
            --color "B60205" \
            --description "Automated production pipeline incident" \
            --force

          issue_number="$(
            gh issue list \
              --repo "$GH_REPO" \
              --state open \
              --label "ops/incident" \
              --search '"Governance pipeline unhealthy" in:title' \
              --json number \
              --jq '.[0].number // empty'
          )"

          if [[ "$INCIDENT" == "true" ]]; then
            if [[ -n "$issue_number" ]]; then
              gh issue edit "$issue_number" --repo "$GH_REPO" --body-file "$REPORT_PATH"
            else
              gh issue create \
                --repo "$GH_REPO" \
                --title "[ops/incident] Governance pipeline unhealthy" \
                --label "ops/incident" \
                --body-file "$REPORT_PATH"
            fi
          elif [[ -n "$issue_number" ]]; then
            gh issue comment "$issue_number" --repo "$GH_REPO" --body-file "$REPORT_PATH"
            gh issue close "$issue_number" --repo "$GH_REPO" --reason completed
          fi

      - name: Fail the workflow when an incident is active
        if: >-
          always() &&
          (
            steps.api_routing.outcome == 'failure' ||
            steps.checkout.outcome == 'failure' ||
            steps.revision_guard.outcome == 'failure' ||
            steps.rollout.outcome == 'failure' ||
            steps.health.outcome == 'failure' ||
            steps.health.outputs.incident == 'true'
          )
        run: |
          echo "::error::Operations watchdog detected an active incident."
          exit 1
